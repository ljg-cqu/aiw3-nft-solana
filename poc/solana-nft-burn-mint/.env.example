# Solana Network Configuration
# Options: "devnet", "testnet", "mainnet-beta", "localnet"
SOLANA_NETWORK="devnet"

# System Wallet Configuration (Backend System Wallet)
# This wallet has minting authority and pays for minting transactions
# Format: Comma-separated array of numbers (e.g., "123,45,67,89,...")
SYSTEM_SECRET_KEY="YOUR_SYSTEM_WALLET_SECRET_KEY_AS_COMMA_SEPARATED_NUMBERS"

# User Wallet Configuration (NFT Owner)
# The user's wallet address (public key) - must match USER_SECRET_KEY
USER_WALLET_ADDRESS="YOUR_USER_WALLET_PUBLIC_KEY"

# User's private key for burning operations
# Format: Comma-separated array of numbers (e.g., "123,45,67,89,...")
USER_SECRET_KEY="YOUR_USER_WALLET_SECRET_KEY_AS_COMMA_SEPARATED_NUMBERS"

# Legacy Environment Variables (for index.js burn-only script)
# NFT mint address for existing NFT to burn
NFT_MINT_ADDRESS="YOUR_EXISTING_NFT_MINT_ADDRESS"

# Payer secret key (for burn-only operations)
# Format: Comma-separated array of numbers (e.g., "123,45,67,89,...")
PAYER_SECRET_KEY="YOUR_PAYER_SECRET_KEY_AS_COMMA_SEPARATED_NUMBERS"

# Security Notes:
# - Never commit actual secret keys to version control
# - Keep secret keys secure and never share them
# - Use different wallets for development and production
# - Ensure USER_WALLET_ADDRESS matches the public key derived from USER_SECRET_KEY